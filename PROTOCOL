ROVER PROTOCOLS
===============

A 'number' is an integer, positive, or negative. We use integers for movement,
and wheel rotation (rotation for turns).

  number ::= (-)[0-9]+

  move_ns ::= movens <number>

  move_ew ::= moveew <number>

  rotate_wheels ::= turn <number>


